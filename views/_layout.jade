include mixins/mixins
  
doctype html
html(ng-controller='PageCtrl', manifest='#{manifest}')
  head
    base(href='/')
    include index/head
    // include metrics/yandex
  body(ng-controller='AppCtrl', insert-chat-sidenav)
    block content

    div.notifications
    div.page__not-ready(ng-if='!isPageReady', layout='row', layout-align='center center', layout-fill)
      div.sent-list__loading(style='display: inline-block;')
        div.content-spin__layer
          div.search-spin-layer_
            div.search-spin
